<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('./partials/head') %>
    <title>Document</title>
    <style>
        body {
            margin: 0px;
            padding: 0px;
            font-family: Arial, Helvetica, sans-serif;
        }

        .container {
            width: 80%;
            height: 100vh;
            margin: 0 auto;
            padding: 0 5px;
            /* border: 1px solid black; */
        }

        .container h2 {
            text-align: center;
            color: #555;
            margin-top: 70px;
        }

        .Form_Outer_Box {
            width: 600px;
            height: 200px;
            margin: 0 auto;
            border-radius: 15px;
            /* border: 1px solid blue; */
        }

        .Form_Outer_Box h4 {
            text-align: center;
            margin-top: 30px;
            color: #555;
        }

        .Form_Outer_Box input {
            width: 70%;
            padding: 10px 8px;
            margin-top: 25px;
            margin-left: 15%;
            border: 0.5px solid #555;
            /* border: none; */
            border-radius: 20px;
            color: #555;
            outline: none;
            background-color: aliceblue;
        }

        .Form_Outer_Box button {
            width: 100px;
            padding: 8px;
            border-radius: 20px;
            outline: none;
            cursor: pointer;
            margin-left: -110px;
            background-color: lightsalmon;
            border: none;
        }

        .short_id h4 {
            text-align: center;
            color: #555;
        }

        .short_id span {
            color: lightsalmon;
        }
    </style>
</head>
<body>
    <%- include('./partials/nav') %>
    <div class="container">
        <h2>URL Shortner App</h2>
        <div class="Form_Outer_Box">
            <h4>Generate Your URL Shortner</h4>
            <form method="post" action="/api/url">
                <input type="text" name="URL" placeholder="http://example.com">
                <button type="submit">Generate</button>
            </form>
        </div>
        <div class="short_id">
            <% if (locals.id) { %>
                <h4>URL Generated:- http://localhost:8000/api/url/<%= id %>
                </h4>
                <% } %>
        </div>
    </div>

    <%- include('./partials/script') %>
</body>
</html>